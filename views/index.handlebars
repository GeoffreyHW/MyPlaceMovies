  <div class="w3-sidebar w3-light-grey w3-bar-block" style="color: white; width:45%; background-color: #08415c; padding: 15px;">
      <form style = 'float: left; padding: 5px; text-align:right;' method="post" action="/api/movies">
          <label>Movie Title</label>
          <input type="text" name="movie">
          <br>
          <label>Score</label>
          <input type="text" name="score">
          <br>
          <label>Thoughts</label>
          <input type = "textarea" name = "thoughts">
          <br>
        <button type="submit" id="addButton" onClick = "window.location.reload()">Add Movie</button>
      </form>
  </div>
  <div class="container">
    {{#if movies}}
        {{#each movies}}
        <div class="card" style="margin: auto; width: 400px; height: 100px; margin-bottom:10px;">
            <p>{{this.title}}: {{this.score}}</p>
            {{this.thoughts}}
        </div>
        {{/each}}
    {{/if}}
  </div>

